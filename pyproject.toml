[project]
name = "bronze"
version = "0.1.0"
description = "Personal AI Employee - Bronze Tier Implementation"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "watchdog>=4.0.0",
]

[project.scripts]
ai-employee = "run:main"
start-watchers = "watchers.filesystem_watcher:main"
run-orchestrator = "orchestrator:main"
